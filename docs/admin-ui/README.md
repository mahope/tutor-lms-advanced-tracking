# Admin UI scaffold (charts + CSV)

Dette scaffold beskriver hvordan en admin-oversigt kan bygges med Chart.js og CSV-eksport.

- Admin menu: Tools > Tracking Overview
- Charts: Chart.js via CDN (kun loaded på admin-siden)
- CSV: WP nonce-beskyttet download-endpoint

Se scripts/admin-ui-scaffold.php for eksempel-kode (ikke auto-loadet i pluginet endnu).
